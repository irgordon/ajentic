# UI Display Specification

TypeScript is visibility only.

## Phase 1 note

No UI behavior is implemented in this phase.

## Future consumption rule

In later phases, UI surfaces must consume records generated by Rust core and must remain non-authoritative.
The UI must not define lifecycle or promotion authority.
