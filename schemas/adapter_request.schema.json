{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Adapter Request Contract",
  "description": "Phase 1 adapter request boundary contract shape.",
  "type": "object",
  "required": [
    "protocol_version",
    "run_id",
    "candidate_request_id",
    "provider",
    "model",
    "objective_ref",
    "constraints_ref",
    "domain_ref",
    "input_ref",
    "limits"
  ],
  "properties": {
    "protocol_version": { "type": "string" },
    "run_id": { "type": "string" },
    "candidate_request_id": { "type": "string" },
    "provider": { "type": "string" },
    "model": { "type": "string" },
    "objective_ref": { "type": "string" },
    "constraints_ref": { "type": "string" },
    "domain_ref": { "type": "string" },
    "input_ref": { "type": "string" },
    "limits": { "type": "array", "items": { "type": "string" } }
  },
  "additionalProperties": false
}
