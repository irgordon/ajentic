[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "ajentic-adapter"
version = "0.0.0"
description = "AJENTIC Python adapter scaffold (v0.0.0)"
requires-python = ">=3.11"
dependencies = []
